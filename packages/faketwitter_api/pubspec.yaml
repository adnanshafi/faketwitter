name: faketwitter_api
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.17.5 <3.0.0'

dependencies:
  models: 
    path: ../models
  mem_cache:
    path: ../mem_cache
  cloud_firestore: ^3.2.1


dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
